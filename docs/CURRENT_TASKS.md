# Текущая очередь — возврат к Godot, 21.09.2026

**Последнее решение:** 21.09.2026 пользователь свернул миграцию на UE5 и вернулся к
Godot-клиентам. Наработки UE удалены из репозитория; их исходники заархивированы вне
репозитория (`~/ue5-archive/ue5-source-20260921.tar.gz`). Не возобновлять UE-работу
без нового явного указания.

## Очередь

- [x] Проверить серверную часть: pipeline 6/6, юнит 51/51, сервер 21/21; прод отвечает `hi`, TLS `wss://realms.neuraldeep.ru/ws`.
- [x] Разобран отказ `running out of the telegraph avoids the actual server hit`. Причина — в самом тесте: замах длится не больше 0.32 с (`server/server.js`, `Math.min(0.32, …)`), а `await create_timer(0.05)` и сохранение PNG в `_screenshot` съедали окно реакции. В headless `_screenshot` выходит сразу, поэтому отказ был виден только в оконном режиме. Движение героя перенесено на момент обнаружения замаха, диагностика — после. Три оконных прогона подряд: 178 проверок, 0 отказов; скриншот `mob-windup.png` по-прежнему показывает сектор, замах и бегущего героя.
- [x] Взяты приёмы из ArcEngine: линтер сцены `npm run native:lint` и единый файл чисел `godot/scripts/tuning.gd` с контрактом `tests/tuning.test.js`; таблица «задача → что читать» в начале AGENTS.md.
- [ ] Пройти `npm run native:verify -- --release` и опубликовать сайт скачивания Godot-клиентов.
- [ ] Собрать клиенты: macOS, Windows, Android, iOS (`npm run native:build -- <target>`). Для мобильных нужны SDK и подпись.

## История: UE5 (свёрнуто)

- [x] Исследовать совместимость текущего Mac: Apple M1, 16 ГиБ, macOS 14.5, Xcode 16.2; UE5.7 совместим по требованиям, Nanite на M1 недоступен. На момент аудита свободно около 27 ГиБ.
- [x] Проверить наличие UE/Epic Launcher: не установлены. Провести аудит места; удаление файлов не выполнялось.
- [x] Зафиксировать решение о UE5, план переноса всех функций и передачу контекста следующему агенту.
- [ ] Выбрать другую машину: существующая/новая, ПК/ноутбук/удалённая, бюджет и страна покупки. Вопросы заданы, ответа пока нет. Не возобновлять очистку текущего Mac автоматически.
- [ ] На выбранной машине закрепить версию UE и инструменты, установить через официальный канал, воспроизвести текущую серверную базу тестов из чистого clone.
- [ ] Создать UE-проект и первый проверенный маршрут: вход → тот же сервер → карта → движение → выбор моба → бой → награда/подбор.
- [ ] Перенести весь UI, модели, анимации, материалы, звук, прогрессию, пати и пользовательские настройки по матрице UE5_MIGRATION.
- [ ] Исправить непрошедшую проверку уклонения после снижения скорости; проверить реальные тайминги/позиции и выбор моба тестом.
- [ ] Добавить конвейер импорта/сборок/серверной приёмки UE, Windows/macOS; отдельно проверить Android/iOS на устройствах.
- [ ] Реализовать автообновление: запрос → фоновая загрузка → кнопка «Установить и перезапустить» → проверка запуска/откат.
- [ ] После приёмки опубликовать согласованные сервер/клиенты/сайт и открыть финальную сборку на основном сервере.

## Что сохранено до переезда

Новые охотничьи участки, сниженные скорости, обновлённые анимации/масштабы, независимые высоты чата, девять PBR-карт, серверная пати и её UI — в коде; [FARMING_AND_PARTY.md](FARMING_AND_PARTY.md). Это **checkpoint, не готовый новый релиз**. Последний опубликованный код — `412b2dd`, выпуск `412b2dd6bb91-20260918T131513506Z`. Персональная выдача уровня 25 и совместимой топовой экипировки двум указанным пользователем персонажам на основном сервере выполнена отдельно; БД и результаты этой операции остаются вне Git.

Проверки до остановки разработки: 46/46 JS, 21/21 сервер; ранний desktop render 178/178 до последних правок размера животных/ограничения чата; ранний аудит 19 моделей и 2 видов добычи до последнего уменьшения животных. Последний touch headless: **177 проверок, 1 отказ** `running out of the telegraph avoids the actual server hit`; исправление переполнения чата в этом прогоне прошло. Эти результаты не заменяют финальный release. Перед сохранением checkpoint дополнительно пройдены 6/6 pipeline-контрактов, хеши 9 PBR-карт и синтаксис существующей правки texture.py; GPU не использовался.

Локальные свидетельства (не входят в clone): `.native-run/farming-unit.log`, `farming-server.log`, `farming-native.log`, `farming-touch-headless.log`, `farming-scales.log`. Повторять проверки командами из NATIVE_PIPELINE; не считать наличие старого отчёта проверкой нового коммита. Существующая правка `tools/trellis/texture.py` включается отдельным коммитом по новой просьбе сохранить всё, без редактирования её содержимого.

# История: интерфейс по утверждённым пользователем референсам

«Надо взять это за основу» — прямое указание после четырёх новых изображений. Реализованы: собственные бронзовые SVG-рамки/кнопки, более компактные статус/цель/панель действий, прозрачный цветной чат с отдельным системным журналом сверху, сумка с 3D-героем и 12 местами экипировки слева / шестиколоночной сеткой справа. Пустые слоты имеют собственные силуэты, действия — пиктограммы, счётчики вещей наложены на иконки. Touch оставляет крупные ячейки и вертикальную компоновку. Tab/I открывает сумку, Q — цель. Неработающие группы/кланы не имитируются.

Код `412b2dd6bb91d2dc48ac2dd6eb47597d11b64e35`, выпуск `412b2dd6bb91-20260918T131513506Z` опубликован. 14/14 этапов, 6 pipeline, 42 rules, 20 server, 166 desktop + 166 touch + 166 packaged с рендером, 10 оружейных поз; warnings: [], changedInputs: []. [CI 35348754951](https://github.com/vakovalskii/deep-chronicles/actions/runs/35348754951) success Windows/macOS: по 165 source/packaged headless, 65 с TLS, 6 pong, 0 reconnect. Реальный drag купленного меча на слот подтверждён сервером; все 12 мест входят в окно. Проверяются также все каналы/личка/прокрутка чата, drag навыков/панели, музыка и микшер.

Windows ZIP: 154651948 байт, macOS: 177402577 байт. Сайт раздаёт новые архивы; основной сервер сообщает combatTelegraphs:1. Логи `ui-reference-release.log`, `ui-reference-ci.log`, `ui-reference-deploy.log`. Полный расчёт публичных SHA-256 сохраняется в `ui-publication.json`. Продакшен-база сохранена серверным backup/promote, не подменялась локальной.

Самостоятельный app открыт и оставлен на обычном локальном сервере, сумка раскрыта: `.native-run/ui-release-running.png` + JSON, authenticated=true, 60 FPS, music_explore playing. Локальный показ не вытесняет игрока Windows. Windows ZIP показан в Finder. Воспроизведение: `npm run native:local -- --built --skip-assets --panel=inventory --capture=/абсолютный/путь/ui.png`. Это CLI-показ UI, прогресс по-прежнему у сервера. OCR не использовался; пользовательская правка `tools/trellis/texture.py` не изменялась и не включена в коммиты.

Границы: скриншоты стали основой UI; храм/архитектура и вся карта пока не переделаны под них. Направление следующего графического прохода — ART_AND_COMBAT_DIRECTION.md; обнаружено перекрытие камеры кронами. Автообновлятор и релизы Android/iOS по-прежнему не реализованы. Не считать headless CI ручной проверкой графики/звука Windows.

## 18.09.2026 — движение/бой/музыка — опубликованный выпуск 94ef273

Новые требования пользователя: улучшать итерационно бег/атаки/мобов, снизить скорость; заменить плохой синтетический звук, добавить музыку и сведение; Tab → сумка; живой онлайн сайта/клиента; расширять тесты и смотреть логи.

Реализовано: скорости 18/16.8, мобы 8.4 (тяжёлые 6.72); Sprint с темпом от фактического смещения, плавный старт; 8 локально воспроизводимых клипов у 5 животных; серверные замахи мобов с фиксированным сектором и возможностью уйти, отложенная автоатака героя с отменой; реакции попадания/локальные HP; камера 21 вместо 28 и меньше тумана. Tab/I → сумка, Q → цель. Сайт держит WS для живого онлайна; клиентский счётчик был готов раньше.

Звук: 67 закреплённых CC0-файлов, 27 cue; три авторские музыкальные темы; два голоса crossfade, ducking в бою, четыре регулятора, master limiter −1 dB. Исходники/лицензии/сведение — SOUND_DESIGN.md. Старые 13 синтетических WAV удалены. OCR не использовался. Чужой tools/trellis/texture.py сохранён.

Выпуск `94ef2734770b-20260918T124810126Z` опубликован. 14/14 этапов release: 6 pipeline, 42 rules, 20 server, по 164 desktop/touch/packaged с реальным рендером, 10 поз оружия, обе сборки, экономика и сайт. Warnings: [], changedInputs: []. CI [35346266115](https://github.com/vakovalskii/deep-chronicles/actions/runs/35346266115) success Windows/macOS: по 163 source/packaged (headless пропускает захват реального аудиосигнала), TLS 65 с, 6 pong, 0 reconnect. Устранён дефект теста cooldown: реальные 80 мс ожидаются через Time.get_ticks_msec, а не ускоренный таймер headless.

Публичные ZIP полностью скачаны и сверены SHA-256 с локальным манифестом: Windows 154635027 байт, macOS 177387548. Файл `.native-run/music-publication.json`; логи `music-release.log`, `music-ci.log`, `music-deploy.log`. Сервер объявляет combatTelegraphs:1. Прогресс не заменялся локальной БД. Свежий app открыт на обычном локальном сервере, чтобы слышать музыку без вытеснения Windows-сессии: `music-release-running.png.json`, 60 FPS, authenticated=true. В verbose Metal-журнале есть штатная конвертация текстур RGB8→RGBA8; обычные release-тесты без предупреждений/ошибок. Автообновлятора нет, Android/iOS не выпускались.

Следом пользователь прислал ещё четыре референса и явно принял их за основу. Проход по компактному HUD, чату и инвентарю выполнен в выпуске 412b2dd; направление и границы записаны в UI_STYLE.md.

# Задачи пользователя — 18 сентября 2026

Источник: последние сообщения пользователя. Основной продукт — самостоятельный Godot-клиент. Браузерную игру больше не поддерживаем. Общие JS-правила остаются источником данных сервера и экспорта.

**Актуальное решение по Git:** после просьбы «а делай его публичным» `vakovalskii/deep-chronicles` переведён в PUBLIC. Полный проект открыт, default branch `server-authoritative`, origin не менялся. Упоминания приватности ниже описывают предыдущие этапы, а не текущую политику. README, AGENTS и GIT_WORKFLOW обновлены. Перед переключением проверены обе ветки и вся достижимая история: Gitleaks 8.30.1 — 0 находок; 561 текстовый blob дополнительно сверён с локальными значениями секретов. Паролей/ключей не найдено; исторические совпадения DEPLOY_HOST в старой main — адрес развёртывания, не данные аутентификации. Файлов .env, приватных ключей или баз в истории не найдено. Самостоятельная правка tools/trellis/texture.py не включена в коммиты.

- [x] Разделить системный журнал и обычный чат; системный разместить над обычным. Настройки отображения, размера и фильтров сохранять.
- [x] Прокрутка чата, окон и миникарты не должна двигать камеру; проверить настоящие события мыши.
- [x] Сделать видимые кнопки действий, включая «Поднять добычу», и настраиваемую панель быстрого доступа с сохранением.
- [x] Добавить уменьшение/увеличение миникарты.
- [x] Найти и исправить парящие объекты; проверять размещение относительно рельефа воспроизводимо.
- [x] Вернуть серверный автолут с переключателем, сохранив ручной подбор и защиту от повторной выдачи.
- [x] Добавить SP за убийства и изучение/ранги навыков за SP в карточке навыков. По умолчанию — ступени уровней, как предпочитаемое направление C4; не обещать новый навык на каждом уровне.
- [x] Продумать оружие и экипировку: ступени, ограничения, способы получения и равнозначную прогрессию воина/мага; устранить пробелы каталога.
- [x] Описать экономику и добавить воспроизводимый расчёт наград/стоимости прогрессии; обозначить ограничения оценки.
- [x] Честно описать сходство и отличия от L2 C4, опираясь на исследование.
- [x] Убрать браузерную игру с сайта и из обязательного конвейера. Сайт оставить для скачивания нативного клиента и статуса сервера.
- [x] Собрать macOS-приложение и Windows EXE в ZIP, проверить содержимое архивов, показать Windows ZIP в Finder.
- [x] Проверить сервер + нативный клиент, обновить основной сервер без замены базы, открыть свежую игру и оставить окно.
- [x] Обновить память и документацию для следующих агентов, сделать коммит. Чужой tools/trellis/texture.py не трогать.

## Начальный аудит

Пока есть только автоматическое открытие трёх навыков на класс по уровню; SP и изучения нет. Грейды сжаты под собственную игру до 40 уровня (D:8, C:18, B:25), это не буквальные уровни C4. В B-каталоге отсутствует равноценное оружие и комплект мага. Экономика основана на таблицах наград и цен, долгосрочный баланс не доказан. В предыдущем релизе добавлен ручной серверный дроп; новый запрос на автолут заменяет прежнюю установку «только ручной подбор».

## Итог выпуска

- Код: `9cd5558`. Основной релиз: `9cd55585de6f-20260918T083251064Z`.
- Полный `native:verify -- --release`: 40 проверок правил, 15 серверных, 95 Godot с рендером, 6 поз оружия; расчёт экономики; обе сборки; проверка сайта. Все шаги успешны, отчёт `.native-run/verification/report.json`.
- Отдельный прогон сенсорной раскладки: 95/95. Это не проверка на физическом телефоне.
- Основной сервер подтверждён через Godot TLS: `groundLoot:1`, `autoloot:1`, `progression:1`, `crafting:1`, `nativeOnly:1`. Локальный сервер также обновлён, DEV_CMD выключен. База продакшена не заменялась.
- Сайт https://realms.neuraldeep.ru раздаёт Windows/macOS ZIP и онлайн. Проверены публичный HTML, совпадение манифеста, размеры и ZIP-сигнатуры обоих файлов (`.native-run/publication.json`). Браузерной игры больше нет.
- Windows: `godot/builds/windows/khroniki-glubin-windows.zip` (~143 МБ), внутри EXE/README/SHA256. Архив открыт в Finder. macOS: `godot/builds/macos/Хроники Глубин.app` и ZIP (~164 МБ).
- Собранное macOS-приложение открыто на основном сервере; контрольный кадр `.native-run/native-release-running.png`, состояние connected=true, authenticated=false, FPS 60. Оставлен экран входа; вход за пользователя не выполнялся.
- Память: AGENTS.md (CLAUDE.md — ссылка), документы Godot/протокола/релиза и онтология обновлены. Чужой `tools/trellis/texture.py` не включён в коммит.

Ограничения: баланс 32–40 требует новых зон; Windows экспорт проверен на macOS, физические Windows/Android/iPhone ещё не проверены. Новый B-каталог использует существующие семейства моделей/иконок. Новые типы оружия и уникальные модели каждого предмета — следующий объём, не готовая функция. Подпись издателя Windows/Apple-нотаризация и автообновлятор пока не настроены.

## Новый запрос: конвейер, Git, Windows и взаимодействие UI

- [x] Аудит Git: origin публичный, deep-chronicles приватный, истории main разные.
- [x] Убрать обход native-конвейера через quick.sh; проверять добавленные/удалённые входы и манифест.
- [x] Добавить воспроизводимую установку Godot/templates с SHA-512 и CI Windows/macOS.
- [x] Реальное перетаскивание изученного навыка из K на панель, обмен ячеек и видимый замок.
- [x] Расширить проверки всех каналов чата и поведения при разрывах.
- [x] Исправить лишнее переподключение при возврате из паузы; heartbeat, backoff, контроль closing, безопасная диагностика.
- [ ] Подтвердить причину конкретного разрыва на компьютере пользователя с Windows: пока нет кода ошибки/журнала.
- [x] Выполнить финальный конвейер, опубликовать исправленные ZIP/сервер, открыть приложение.
- [x] Отправить полный проект в приватный Git и проверить удалённый CI.
- [x] Исправить трактовку «оср»: это OCR-бокс, а не OSS. Не публиковать игровой сервер на основании этой просьбы; полный проект приватный.

Первоначальная трактовка «верни оср сервер обратно под оср пайп» как OSS оказалась ошибочной: речь об OCR-сервере, занятом генерацией. Текущее состояние см. в разделе OCR ниже. Чужой tools/trellis/texture.py по-прежнему вне коммитов.

## Выпуск исправлений конвейера и связи

- Проверенный код: `6495562e6e12545a10d5dd3a6cf22f963bb365e7`. Релиз: `6495562e6e12-20260918T093707232Z`.
- Локальный полный конвейер: **13 успешных шагов**, 6 pipeline-contract тестов, 40 правил, 17 серверных; **121/121** Godot desktop, **121/121** touch, **121/121** упакованного macOS-приложения; 6 оружейных проверок; экономика, обе сборки и сайт. Исходники во время проверки не изменялись (`changedInputs: []`). Отчёт: `.native-run/verification/report.json`, лог `.native-run/verified-release.log`.
- Удалённый CI **success** на Windows и macOS: [run 35329881740](https://github.com/vakovalskii/deep-chronicles/actions/runs/35329881740). Обе ОС самостоятельно установили закреплённый движок, проверили исходники, экспортировали пакет, выполнили **121/121 проверку самого EXE/app**, затем 65 секунд TLS через настоящий Network без переподключений. Это headless-проверка ОС, не проверка видеодрайвера компьютера пользователя.
- Воспроизведена и исправлена конкретная причина цикла: kicked мог теряться при одновременном закрытии сокета. Читаются последние пакеты; сервер использует код 4001; прежний клиент остаётся на экране входа с объяснением и не забирает персонажа обратно. Также исправлены ложный reconnect при возврате фокуса, таймаут closing, heartbeat и обработка ошибок отправки. Причина разрывов именно на компьютере пользователя не подтверждена его журналом.
- Перетаскивание реально проверяется press/motion/release: иконка из K → пустая ячейка, обмен ячеек, запрет при замке. Чат: мир/торг/рядом/ЛС, дистанция, cooldown, /w, /r, кавычка, unread, фильтры, сохранение истории/прокрутки/черновика, камера не двигается.
- Приватный `deep-chronicles` получил все коммиты; default branch = `server-authoritative`. Локальные origin/upstream/pushDefault ведут туда, публичный remote называется oss. Старые ветки не перезаписывались. `docs/GIT_WORKFLOW.md` — актуальный порядок.
- OSS-снимок сервера из HEAD отдельно подготовлен и проверен: **17/17**, `.native-run/oss-server`, `.native-run/oss-verified.log`. В публичный репозиторий ничего не отправлялось. Позже выяснилось, что просьба про «оср» относится к OCR; этот OSS-экспорт не является требованием пользователя.
- Основной сервер и https://realms.neuraldeep.ru обновлены, `heartbeat:1` подтверждён. Windows ZIP 149466209 байт; macOS ZIP 172217402 байта. Публичные манифест, размеры и ZIP-сигнатуры проверены (`.native-run/publication.json`). Продакшен-база не заменялась; backup и rollback — на сервере.
- На Mac открыто новое самостоятельное приложение, **без --resume**, чтобы не вытеснять игрока с Windows. Контрольный кадр `.native-run/current-release-running.png` / `.png.json`: connected=true, authenticated=false, 60 FPS. PID записан в `.native-run/client.pid`; Windows ZIP показан в Finder. Обычный локальный сервер тоже перезапущен с новым кодом, DEV_CMD выключен.
- Автообновление: пользователь получил схему отдельного лаунчера, подписанного манифеста, загрузки в staging, переключения версий и отката. `docs/AUTO_UPDATE.md`. **Лаунчер не реализован**; сейчас остаётся ручное скачивание ZIP.
- Незавершённая пользовательская правка `tools/trellis/texture.py` сохранена отдельно и не включена в коммиты.

После promotion выполнен ещё один 65-секундный probe упакованного клиента к основному серверу: heartbeat=1, **6 pong**, **0 reconnect**, TLS=true (`.native-run/live-release-probe.log`).
## Боевой первый проход, звук и счётчик — 18.09.2026

- [x] Собственные звуки боя/заклинаний/шагов/добычи/уровня и тихий фон; 13 воспроизводимых WAV, 3 регулятора громкости, ограничение голосов.
- [x] Отдельные подготовка/удержание/выпуск каста, полная атака, реакция на попадание, след оружия; эффекты огня, льда, лечения, крита.
- [x] Чужой каст передаётся сервером только соседям, не блокирует своего героя; совместимость со старым клиентом через cast_start.
- [x] Явный живой «Игроков онлайн» на входе и в игре, без учёта анонимных подключений.
- [x] Полная версия в приватном deep-chronicles подтверждена через GitHub; публичный старый репозиторий всё ещё открыт и не менялся.
- [x] 139 native-проверок с рендером, 18 server, 6 оружейных; verify --build=macos прошёл, changedInputs: [].
- [ ] Оркестровая музыка, записанные голоса и foley, ручная доводка анимаций/оружия; законченный архитектурный участок город → дорога → руины. Это следующий художественный этап, не готовая часть этого выпуска.

Направление, происхождение ресурсов, ограничения и воспроизведение: `docs/ART_AND_COMBAT_DIRECTION.md`. Данные/урон/экономика и серверный мир не перенесены в клиент. Изменение tools/trellis/texture.py по-прежнему чужое.

Итог: код **76f8d5376c4c5a391a31f21b09cfb8a8807f776c** в приватном Git; релиз **76f8d5376c4c-20260918T103934542Z** опубликован 18.09.2026 10:43 UTC. Полный конвейер 14/14, включая audio-assets, 6 pipeline/40 rules/18 server, 139 desktop/139 touch/139 packaged, 6 weapons, обе сборки и сайт; changedInputs: []. [CI 35335513667](https://github.com/vakovalskii/deep-chronicles/actions/runs/35335513667) success на Windows/macOS: на каждой ОС исходники и упакованный клиент по 139 проверок, 65 с реального TLS, 6 pong, 0 reconnect. Это не ручная проверка графики Windows или мобильных устройств.

Публичные файлы сверены с локальным release.json: Windows ZIP **149637305** байт, macOS ZIP **172387349** байт, HTTP 200, PK0304; SHA в `.native-run/combat-publication.json`. Основная БД сохранена на сервере, не заменялась. Логи `.native-run/combat-release.log`, `combat-deploy.log`, `combat-ci.log`. Приложение открыто на входе без --resume, чтобы не вытеснять аккаунт Windows: `.native-run/combat-release-running.png` и JSON подтверждают connected=true, authenticated=false, online=2, 60 FPS. PID в client.pid; обычный локальный сервер перезапущен с новым кодом без DEV_CMD, PID в server.pid.

Автообновления по-прежнему нет: пользователю Windows нужно скачать новый ZIP. Android/iOS не выпускались. Публичный OSS-архив не изменялся. Атмосфера всей карты, ручная анимация и записанный звук ещё требуют следующего художественного прохода; этот выпуск — боевая подача и первый звуковой слой.

## OCR-бокс возвращён в production — 18.09.2026

- Исправлена существенная ошибка понимания: «оср сервер / оср пайп / оср бокс» — это OCR распознавания документов, а не OSS-публикация игрового сервера.
- При аудите OCR был остановлен, GPU занят trellis2-api (~16 ГБ), все 28 задач генерации завершены. После проверки пустой очереди и принадлежности контейнера игре TRELLIS остановлен.
- Запущены прежние MongoDB, MinIO, vLLM, worker, две API-реплики и Traefik. Все healthchecks здоровы; HTTP healthz/readyz/profile = 200. PaddleOCR-VL-1.6-0.9B / paddle-vllm / standard-ocr. Конфигурация, образы, секреты и volumes сохранены.
- Полный OCR smoke через production API завершился: `job_4ca59d4e29c5fa4bfe4924df`, 10384 мс, точное совпадение тестового текста. Никаких пользовательских документов или webhook.
- Воспроизводимые `tools/trellis/ocr-box.mjs` + `ocr_box.py`: --status, --restore, --smoke. Инструкции/свидетельства: `docs/OCR_BOX.md`. Не занимать этот GPU генерацией без отдельного указания; он принадлежит production OCR.
- Игровой релиз 76f8d53 остаётся на проде; новая сборка клиента для этого исправления не нужна. Полный проект приватный, публичный OSS-архив не менялся. Чужая правка tools/trellis/texture.py сохранена.


## Текущий проход: охота, пати, атмосфера, масштаб, чат

- [x] Раздельное изменение высоты системного/обычного чата мышью и настройками, сохранение и запрет движения камеры при drag.
- [x] Уменьшить общие скорости героя и мобов, заменить спринт бегом трусцой, согласовать замах с серверным ударом, добавить вторую атаку.
- [x] 70 дополнительных стартовых мобов в семи свободных охотничьих участках, бесплатное перемещение к первой группе.
- [x] Аудит размеров 19 моделей и добычи, уменьшение животных/монет/мешка/иконок, соответствующие коллизии.
- [x] PBR-карты камня/земли/кладки, приглушённая палитра и тёплые огни; лицензии и воспроизводимый импорт/проверка.
- [x] Пати до шести, доли XP/SP, random/last_hit/pickup, приглашения/лидер/выход и канал чата; серверные и нативные проверки.
- [x] По отдельному указанию изменены два запрошенных профиля на основном сервере: уровень 25, лучший совместимый магический комплект +16; старые вещи сохранены. Результат операции и повторное чтение — .native-run/admin-grant-verified.json, резервная копия на сервере.
- [ ] Финальный полный release-конвейер, публикация проверенных клиента/сервера, открытие приложения на основном сервере.

Автообновление по-прежнему спроектировано, не реализовано. Ранее было предложено оставить Godot как клиент; последующее прямое решение пользователя — UE5 (см. начало документа). Число одновременных игроков не подтверждено нагрузочным тестом, нужны зоны сервера, пространственная выборка и более строгая серверная симуляция перемещения. Новая храмовая архитектура не заявлена готовой.
