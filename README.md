# Хроники Глубин

Нативная MMORPG: два города, открытая карта, зоны охоты, катакомбы, прокачка, экипировка, заточка, PvP и чат. Действующий клиент — **Godot 4.7.2** (Windows, macOS, Android, iOS). Сервер на Node.js 22 + WebSocket + SQLite управляет боем, аккаунтами и прогрессом.

Миграция на Unreal Engine 5 свёрнута 21.09.2026: наработки удалены из репозитория, разработка продолжается на Godot. [TODO / состояние проверок](docs/CURRENT_TASKS.md). Текущие изменения охоты/пати/чата — промежуточная версия, полный релиз не пройден. Приведённые ниже команды запускают клиент в `godot/`.

Браузерная игра закрыта. На сайте — скачивание клиентов и статус сервера; исходники старого UI оставлены как архив.

## Запуск

```bash
npm ci
npm run dev                          # Godot → основной сервер, вход в аккаунт
npm run native:local                 # отдельная локальная игра
npm run native:verify -- --release    # полный конвейер + macOS/Windows ZIP + сайт
npm run native:editor                # открыть проект в редакторе
npm test                             # правила + сервер + игра в Godot
npm run native:build -- macos         # самостоятельное приложение
npm run native:build -- windows       # Windows x86_64
```

Нужен Godot 4.7.2; путь можно задать через `GODOT_BIN`. Запуск автоматически экспортирует данные из общих JS-модулей и импортирует ресурсы. Обычный запуск подключается к `wss://realms.neuraldeep.ru/ws`: войдите тем же аккаунтом, что и в браузере. Локальный режим включается только явно (`native:local`); его сохранения — `.native-run/world.db`. Токены разных серверов хранятся раздельно. Адрес другого сервера: `npm run native -- --server=wss://example.org/ws`.

Presets Android/iOS находятся в `godot/export_presets.cfg`. Для Android нужны Android SDK/JDK; для iOS — Xcode и Apple signing team. Эти цели требуют отдельной проверки на физических устройствах. Экспортные шаблоны должны соответствовать версии движка.

## Графика

Герои, 13 видов мобов, NPC, дома, храм, башни, фонтан, деревья и камни используют текстурированные GLB. У персонажей есть скелет и клипы движения/боя. Модели размещаются поверх прежней карты с теми же серверными координатами и препятствиями. Земля смешивает шесть рисованных материалов; свет и окружение доступны в сцене Godot.

- `godot/assets/manifest.json` — реестр моделей; `godot/assets/` — исходные игровые ресурсы.
- `art/concepts/` и [art/README.md](art/README.md) — концепты, происхождение и генерация.
- `npm run native:art -- <id>` — подготовка завершённого результата TRELLIS через Blender.
- `npm run native:gallery -- --models=warrior,mage,wolf` — контрольный рендер моделей.
- `godot/generated/` пересоздаётся через `npm run native:assets` и не редактируется вручную.

Повторяемая сборка, проверки клиента с сервером и отчёты: [docs/NATIVE_PIPELINE.md](docs/NATIVE_PIPELINE.md).

Полный порядок работы для следующих агентов: [AGENTS.md](AGENTS.md). Подробности серверного протокола и старого клиента: [CLAUDE.md](CLAUDE.md).

Полный проект открыт в [deep-chronicles](https://github.com/vakovalskii/deep-chronicles), ветка `server-authoritative`; порядок работы с Git: [docs/GIT_WORKFLOW.md](docs/GIT_WORKFLOW.md). Схема будущего автообновления: [docs/AUTO_UPDATE.md](docs/AUTO_UPDATE.md), лаунчер пока не реализован.

Страница скачивания, статус сервера и выпуск обновлений: [docs/DISTRIBUTION.md](docs/DISTRIBUTION.md). Направление интерфейса: [docs/UI_STYLE.md](docs/UI_STYLE.md).

## Управление

WASD или ЛКМ — идти; ПКМ/два пальца — камера; колесо/щипок — масштаб; 1–9 и 0 — назначенные ячейки; «Панель…» меняет назначения; Z — подбор; Tab/I — сумка; Q — цель; F — атака; E — разговор; C — герой; K — умения; M — карта; Esc — меню; V — камера; F11 — полный экран; Enter — чат. На телефоне есть джойстик и кнопки. Перетаскивание и двойной щелчок работают в инвентаре. `/w Имя текст` — личное сообщение, `/r` — ответ.

## Сайт и прогрессия

https://realms.neuraldeep.ru — скачивание Windows/macOS ZIP и текущий онлайн. `npm run site:build` собирает `dist/` из `site/` и уже экспортированных ZIP; `npm run test:site` проверяет страницу на отдельном сервере. `npm run deploy -- --verified` публикует только успешно проверенный релиз.

Автолут можно выключить рядом с панелью действий. SP за мобов расходуются в карточке навыков K. Рецепты оружия и брони — у торговца; путь экипировки — в меню. Правила: [docs/PROGRESSION.md](docs/PROGRESSION.md), расчёт экономики: [docs/ECONOMY_BALANCE.md](docs/ECONOMY_BALANCE.md).

## Лицензия

Код: [AGPL-3.0](LICENSE). Сведения о моделях, анимациях и текстурах: [art/README.md](art/README.md).
